puppet::master::r10k::basegiturl: 'git@github.com:jaxxstorm/puppet-homelab.git'
consul::config_hash:
  datacenter: "home"
  client_addr: "0.0.0.0"
  node_name: "%{::fqdn}"
  bootstrap_expect: 3
  server: true
  data_dir: "/opt/consul"
  ui_dir: "/opt/consul/ui"
  recursors: ["192.168.4.1"]
